# Ultra-minimal requirements for Railway deployment
# Only essential packages that work reliably

# Core dependencies
numpy==1.24.3
opencv-python==4.8.1.78
Pillow==10.1.0

# 3D processing (lightweight)
trimesh==4.0.5
scikit-image==0.22.0

# TTS (lightweight)
TTS==0.22.0

# PyTorch (CPU only)
torch==2.1.1
torchaudio==2.1.1

# Audio processing
librosa==0.10.1
soundfile==0.12.1

# Vector database (lightweight)
chromadb==0.4.18
sentence-transformers==2.2.2
faiss-cpu==1.7.4

# Web framework
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6

# Utilities
python-dotenv==1.0.0
requests==2.31.0
aiofiles==23.2.1
